version: '3.8'

services:
  frontend:
    build:
      context: .
      args:
        - VITE_API_URL=${VITE_API_URL}
    ports:
      - "3000:80"
    env_file:
      - .env
    networks:
      - hotel_network 

networks:
  hotel_network:
    external: true  